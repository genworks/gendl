@subsubsection Global-filleted-polygon-projection

@b{Description}

Similar to a global-polygon-projection, but the polygon is filleted
as with global-filleted-polygon.

@noindent @b{Optional-input-slots}

@b{:default-radius}

@itemize {}

 @item Number. Specifies a radius to use for all vertices. Radius-list will take precedence over this.

@end itemize

@b{:radius-list}

@itemize {}

 @item List of Numbers. Specifies the radius for each vertex (``corner'') of the filleted-polyline.

@end itemize





@noindent @b{Examples}

@smallexample 
@cartouche
 (in-package :gdl-user)

 (define-object global-filleted-polygon-projection-sample 
     (global-filleted-polygon-projection)
     :computed-slots
     ((display-controls (list :color :blue-steel 
                              :transparency 0.3 
                              :shininess 0.7 
                              :spectral-color :white))
      (default-radius 5)
      (projection-depth 5)
      (vertex-list (list (make-point 0 0 0)
                         (make-point 10 10 0)
                         (make-point 30 10 0)
                         (make-point 40 0 0)
                         (make-point 30 -10 0)
                         (make-point 10 -10 0)
                         (make-point 0 0 0)))))

 (generate-sample-drawing :objects 
                          (make-object 'global-filleted-polygon-projection-sample)
                          :projection-direction :trimetric)





@end cartouche 
@end smallexample

@center @image{/home/dcooper8/gdl-build/dcooper8/genworks-gdl/documentation/unified-doc/yadd/geom-base/image/global-filleted-polygon-projection,,4.2in}

